# Flow System

Flow is a sequence of real Screens.

Example:

```
Asset Request
  ↓
Asset Gallery
  ↓
Component Editor
  ↓
World Assembly
  ↓
Runtime Preview
```

Rules:

- No text-only flow nodes.
- Every step maps to a Screen.
- Every success state requires evidence.
